let arreglo = [1, 2, 3, 4, 7, 3, 1, 2, 3]
/*
Agregar un nuevo valor al FINAL del arreglo
arreglo.push(elemento)

Elimina el ÚLTIMO valor del arreglo
arreglo.pop()

Obtener un valor del arreglo
arreglo[indice]

Si el indice se sale del rango de datos del arreglo
el valor que resulta es undefined

Obtener el tamaño de un arreglo
arreglo.length
*/

for (let i = 0; i < arreglo.length; i++) {
    console.log("El índice " + i + " es: " + arreglo[i])
}